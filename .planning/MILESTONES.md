# Milestones: Declare v2.0

## Milestones

| ID   | Title                                                                | Description                                                                                                                                                                                           | Status  | Realizes         | Plan | Review   |
|------|----------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------|------------------|------|----------|
| M-01 | Project scaffold with Bun + Vite + React 19 + Hono                   | Working dev server that serves a React app through Hono, with Tailwind 4, @deco/ui tokens, and TypeScript strict mode. `bun dev` starts both client and server with hot reload.                       | PENDING | D-02, D-06       | NO   | approved |
| M-02 | DAG engine ported to TypeScript with full test coverage              | DeclareDag class ported from v1's engine.js to TypeScript with Vitest tests covering nodes, edges, validation, topological sort, and wholeness computation. Zero runtime dependencies.                | PENDING | D-01, D-06       | NO   | approved |
| M-03 | Artifact parsers for FUTURE.md, MILESTONES.md, PLAN.md               | TypeScript parsers/writers for all three markdown formats. Round-trip tests (parse → write → parse) pass for all edge cases including empty files, hand-edited content, and multi-value fields.       | PENDING | D-01, D-06       | NO   | approved |
| M-04 | Hono API routes serve graph data and handle mutations                | GET /api/graph returns full DAG. POST/PUT/DELETE endpoints for declarations, milestones, actions. SSE /events endpoint broadcasts changes. All routes tested with supertest or equivalent.            | PENDING | D-01, D-04       | NO   | draft    |
| M-05 | Playwright E2E test harness with lifecycle smoke test                | Playwright configured, dev server auto-starts for tests, and a single smoke test loads the dashboard and verifies it renders without JS errors. CI-ready with GitHub Actions workflow.                | PENDING | D-07             | NO   | draft    |
| M-06 | Lifecycle view renders declarations with uniform card components     | Dashboard shows declarations as cards using @deco/ui patterns. Each card has identical Approve/Edit/Delete actions. Batch "Approve All" works. Drill navigation (click declaration → see milestones). | PENDING | D-01, D-02       | NO   | draft    |
| M-07 | Milestone and action cards with consistent approve/review/execute UX | Milestone cards and action cards use the same component base as declaration cards. Approve, Review, Write, Delete actions are uniform. Status badges consistent across all three levels.              | PENDING | D-01, D-02       | NO   | draft    |
| M-08 | CLI entry point: `dcl` starts server, `dcl init` scaffolds           | CLI dispatcher handles `dcl` (open dashboard), `dcl init` (scaffold .planning/), `dcl serve` (start server), `dcl status` (print graph health). Blocks correctly, reuses existing server.             | PENDING | D-04, D-01       | NO   | draft    |
| M-09 | Agent runner spawns Claude subagents with streaming output           | Agent runner wraps Claude Agent SDK or AI SDK. Spawns agents with context (declaration → milestone → action chain). Streams output via SSE. Dashboard shows running agents with their prompts.        | PENDING | D-05             | NO   | draft    |
| M-10 | Meta-prompts as standalone markdown files exposed via MCP            | Six workflow prompts (vision, declarations, milestones, actions, execution, verification) live in src/agents/prompts/. MCP server exposes them as resources. Each is self-documenting.                | PENDING | D-03, D-04       | NO   | draft    |
| M-11 | MCP server exposes tools, resources, and prompts                     | MCP server using @modelcontextprotocol/sdk exposes: tools (add-declaration, derive-milestones, execute, verify), resources (graph state, meta-prompts, project context), prompts (onboarding flow).   | PENDING | D-04, D-03       | NO   | draft    |
| M-12 | Onboarding flow: vision → questions → first declarations             | Dashboard guides new users through: describe your vision → AI generates clarifying questions → user answers → AI generates declaration candidates → user approves. Works from both dashboard and MCP. | PENDING | D-01, D-02       | NO   | draft    |
| M-13 | E2E test covers complete lifecycle                                   | Playwright test runs: init → onboard → declare → derive milestones → plan actions → execute → verify. Asserts graph state at each stage. No manual intervention needed.                               | PENDING | D-07, D-01       | NO   | draft    |
| M-14 | Agent monitor panel shows running/completed agents                   | Side panel lists all agents (running, completed, failed). Click to see prompt and streaming output. Agents persist across page refresh via agent-state.json.                                          | PENDING | D-05, D-02       | NO   | draft    |
| M-15 | Production build and npm publish as declare-cc@2.0.0                 | `bun run build` produces distributable package. `npx dcl` works from any directory. Claude Code plugin integration via install script. Version 2.0.0 published to npm.                                | PENDING | D-01, D-04, D-06 | NO   | draft    |
